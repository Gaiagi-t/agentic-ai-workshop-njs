{
  "env": [
    {
      "key": "OPENAI_API_KEY",
      "description": "OpenAI API key per GPT-4o-mini e Whisper",
      "required": true
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": ".next"
}
